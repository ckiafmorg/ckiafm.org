<% if Current.user.signed_in? %>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="/admin">CKIA - Admin</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
          </li>
          <% if Current.user.admin? %>
            <li class="nav-item">
              <%= link_to t('nav.shows'), admin_emissions_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to t('nav.show_categories'), admin_categorie_emissions_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to t('nav.tags'), admin_tags_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to t('nav.balados'), admin_balados_path, class: 'nav-link' %>
              </li>
            <li class="nav-item">
              <%= link_to t('nav.users'), admin_users_path, class: 'nav-link' %>
            </li>
            <%# <li class="nav-item"> %>
              <%#   <a class="nav-link" href="#">Équipe</a> %>
              <%# </li> %>
            <li class="nav-item">
              <%= link_to t('nav.ads'), admin_publicites_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to t('nav.membres'), admin_membres_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to t('nav.memberships'), admin_memberships_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to t('nav.articles'), admin_articles_path, class: 'nav-link' %>
            </li>
          <% end %>
        </ul>
        <ul class="navbar-nav ms-auto">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarUserDropdownLink" role="button" data-bs-toggle="dropdown" aria-expanded="false"><%= Current.user.email %></a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarUserDropdownLink">
              <li><%= link_to "#{t('auth.signout_button_text')} #{bootstrap_icon('door-open')}".html_safe, session_path, data: {turbo_method: :delete }, class: 'nav-link' %></li>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </nav>
<% end %>
